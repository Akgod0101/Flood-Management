# FloodGraph Environment Configuration
# Map basemap uses Esri World Dark Gray Canvas by default (zero configuration / zero API key required).
# Optional: If you use CARTO Vector / Stadia / Mapbox tiles, add custom tokens here.
NEXT_PUBLIC_CARTO_API_KEY=
